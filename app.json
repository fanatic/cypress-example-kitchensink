{
  "stack": "heroku-20",
  "environments": {
    "test": {
      "env": { "ELECTRON_RUN_AS_NODE": "1"},
      "buildpacks": [
        { "url": "heroku/nodejs" },
        { "url": "heroku/google-chrome" }
      ],
      "scripts": {
        "test-setup": "whoami && node -e 'console.log(process.geteuid())' && npx cypress install && npx cypress cache path && npx cypress cache list && npx cypress verify --browser chrome || true",
        "test": "whoami && node -e 'console.log(process.geteuid())' && npm run ci:set-port && npm run test:ci:record"
      }
    }
  }
}
